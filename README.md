# Vue Calculator Project
Простой калькулятор на Vue 3 с поддержкой базовых операций (+, -, *, /), ввода отрицательных чисел.

## Требования
- Node.js >= 16
- npm >= 8
- 
## Установка проекта
Установите все зависимости:

npm install

##Для запуска используйте команду:

npm run dev
